include ./mixins.pug

<!DOCTYPE html>
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta&attributes({'http-equiv': 'X-UA-Compatible', 'content': 'ie=edge'})
    title My Portfolio
  body
    .wrapper(style=`background-image:url(${require("images/parallax/mountain.jpg")})`)
      .maincontent
        include components/header
        include components/welcom
        include components/hero
        include components/benefit
        include components/job
        .maincontent__autmn-bg
          include components/reviews
          include components/form
          include components/footer



                      